<h1>New Book</h1>
<%= form_with model: @book do |f| %> <!-- controllerを通してmodelの変数を受け渡している
-->
  <h4>Title</h4>
  <%= f.text_field :title %> <!-- title column へ格納するためのフォーム-->

  <h4>Body</h4>
  <%= f.text_area :body %>

  <%= f.submit 'Create Book' %>
<% end %>